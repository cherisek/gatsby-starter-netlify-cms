backend:
  name: git-gateway
  branch: master
  commit_messages:
    create: 'Create {{collection}} “{{slug}}”'
    update: 'Update {{collection}} “{{slug}}”'
    delete: 'Delete {{collection}} “{{slug}}”'
    uploadMedia: '[skip ci] Upload “{{path}}”'
    deleteMedia: '[skip ci] Delete “{{path}}”'

media_folder: static/img
public_folder: /img

collections:
  - name: "Homepage"
    label: "Homepage"
    folder: "src/pages/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Template Key", name: "templateKey", widget: "hidden", default: "blog-post"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "text"}
      - {label: "Featured Post", name: "featuredpost", widget: "boolean"}
      - {label: "Featured Image", name: "featuredimage", widget: image}
      - {label: "Body", name: "body", widget: "markdown"}
      - {label: "Tags", name: "tags", widget: "list"}

  - name: "pages"
    label: "Pages"
    extension: "json"
    editor: 
      preview: false
    files:
      - file: "data/config.json"
        label: "Test Page"
        name: test
        fields:
          - {label: "Template Key", name: "templateKey", widget: "hidden", default: "index-page"}
          - {label: Title, name: title, widget: string, default: false}
          - {label: Image, name: image, widget: image}
          - {label: Heading, name: heading, widget: string}
          - {label: Subheading, name: subheading, widget: string}
      
      # - file: "data/config.json"
      #   label: "test"
      #   name: test
      #   fields:           
      #     - {label: "Template Key", name: "templateKey", widget: "hidden", default: "test-page"}
          - {label: "Title", name: "title", widget: "string"}
          # - {label: "Heading", name: "heading", widget: "string"}
          # - {label: "Hero Headline Bottom", name: "headline bottom", widget: "string"}
          # - {label: "Hero Headline Text", name: "headline subhead", widget: "string"}





